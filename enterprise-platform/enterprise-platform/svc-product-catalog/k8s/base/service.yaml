apiVersion: v1
kind: Service
metadata:
  name: product-catalog
  namespace: ${NAMESPACE}
  labels:
    app: product-catalog
    env: ${ENVIRONMENT}
spec:
  type: ClusterIP
  selector:
    app: product-catalog
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
