apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: ${NAMESPACE}
  labels:
    app: notification
    env: ${ENVIRONMENT}
spec:
  type: ClusterIP
  selector:
    app: notification
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
