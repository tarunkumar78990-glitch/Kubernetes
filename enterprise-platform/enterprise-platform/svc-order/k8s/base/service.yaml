apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: ${NAMESPACE}
  labels:
    app: order
    env: ${ENVIRONMENT}
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
