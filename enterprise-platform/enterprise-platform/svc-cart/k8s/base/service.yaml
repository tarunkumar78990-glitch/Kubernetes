apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: ${NAMESPACE}
  labels:
    app: cart
    env: ${ENVIRONMENT}
spec:
  type: ClusterIP
  selector:
    app: cart
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
