apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-auth
  namespace: ${NAMESPACE}
  annotations:
    # This binds the K8s SA to the GCP SA created by the
    # workload-identity Terraform module. Get the exact value from:
    #   terraform output workload_identity_service_accounts
    iam.gke.io/gcp-service-account: ${ENVIRONMENT}-user-auth@${PROJECT_ID}.iam.gserviceaccount.com
  labels:
    app: user-auth
automountServiceAccountToken: true
