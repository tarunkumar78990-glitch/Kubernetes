apiVersion: v1
kind: Service
metadata:
  name: user-auth
  namespace: ${NAMESPACE}
  labels:
    app: user-auth
    env: ${ENVIRONMENT}
spec:
  type: ClusterIP
  selector:
    app: user-auth
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
